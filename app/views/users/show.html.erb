

<%= flash[:notice] %>


<div class="container"> <!-- ここを追加 -->
    <div class="row">     <!-- ここを追加 -->
  <div class="col-xs-3" style="background-color: white;"> 
    <h2>User info</h2>
     <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg", size: "100x100" %>


    <table class="table">
      <tbody><tr></tr> 
      <tr>
    	  <th>name</th>
    	  <th><%= @user.name %></th>
      </tr>
      <tr>
    	  <th>introduction</th>
    	  <th><%= @user.introduction %></th>
      </tr>
      </tbody>
    </table>

   
    <%= link_to "", edit_user_path(@user.id), class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench"%>
  
   



    

        
        <h1>New book</h1>

<%= form_for(@book) do |f| %>

  <div class="field ">
    <%= f.label :title %><br>
    <%= f.text_field :title, id: :book_title %>
  </div>

  <div class="field ">
    <%= f.label :body %><br>
    <%= f.text_area :body, id: :book_body %>
  </div>


　<div class="actions">
    <%= f.submit %>
  </div>
<% end %>








      </div>



      <div class="col-xs-9">  
<table class="table table-striped">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>opinion</th>
      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= link_to user_path(@user.id) do%><%= attachment_image_tag User.find(book.user_id), :profile_image, :fill, 30, 30, fallback: "no_image.jpg", size: "30x30" %><% end %></td>
        <td><%= link_to book_path(book.id)do %><%= book.title %><% end %></td>
        <td><%= book.body %></td>

      </tr>
    <% end %>
  </tbody>
</table>
     </div>   
  </div>  

  <%= render "books/footer" %>

